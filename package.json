{
  "name": "piggyback",
  "version": "0.8.0",
  "description": "A resource-oriented wrapper over the fetch API",
  "main": "index.js",
  "scripts": {
    "compile": "babel piggyback.js > index.js",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha --compilers js:babel-core/register tests.js"
  },
  "dependencies": {
    "cross-fetch": "^1.1.1",
    "pluralize": "^1.2.1"
  },
  "author": "Mark Rickerby",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.1.18",
    "babel-cli": "^6.2.0",
    "babel-core": "^6.3.2",
    "babel-preset-es2015": "^6.1.18",
    "chai": "^3.4.1",
    "mocha": "^2.3.4",
    "nock": "^3.3.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/digivizer/piggyback"
  }
}
